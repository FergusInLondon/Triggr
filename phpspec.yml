suites:
    events:
        namespace: FergusInLondon\Triggr
        psr4_prefix: FergusInLondon\Triggr
        src_path: src
        spec_path: test

code_coverage:
  format:
    - clover
  output:
    clover: build/logs/clover.xml

#extensions:
#    - PhpSpec\Extension\CodeCoverageExtension